#!/bin/bash

echo "Updating optimizer statistics..."

#@VACUUMDB@ -z $SID
@MYSQL_CLIENT@ -D $SID -e "analyze table supplier"
@MYSQL_CLIENT@ -D $SID -e "analyze table part"
@MYSQL_CLIENT@ -D $SID -e "analyze table partsupp"
@MYSQL_CLIENT@ -D $SID -e "analyze table customer"
@MYSQL_CLIENT@ -D $SID -e "analyze table orders"
@MYSQL_CLIENT@ -D $SID -e "analyze table lineitem"
@MYSQL_CLIENT@ -D $SID -e "analyze table nation"
@MYSQL_CLIENT@ -D $SID -e "analyze table region"

#!/bin/bash

echo "Creating indexes..."
@PSQL@ -d $SID -c "CREATE INDEX i_l_shipdate ON lineitem (l_shipdate);" || exit 1
@PSQL@ -d $SID -c "CREATE INDEX i_l_suppkey_partkey ON lineitem (l_partkey, l_suppkey);" || exit 1
@PSQL@ -d $SID -c "CREATE INDEX i_l_partkey ON lineitem (l_partkey);" || exit 1
@PSQL@ -d $SID -c "CREATE INDEX i_l_suppkey ON lineitem (l_suppkey);" || exit 1
@PSQL@ -d $SID -c "CREATE INDEX i_l_receiptdate ON lineitem (l_receiptdate);" || exit 1
@PSQL@ -d $SID -c "CREATE INDEX i_l_orderkey ON lineitem (l_orderkey);" || exit 1
@PSQL@ -d $SID -c "CREATE INDEX i_l_orderkey_quantity ON lineitem (l_orderkey, l_quantity);" || exit 1
@PSQL@ -d $SID -c "CREATE INDEX i_c_nationkey ON customer (c_nationkey);" || exit 1
@PSQL@ -d $SID -c "CREATE INDEX i_o_orderdate ON orders (o_orderdate);" || exit 1
@PSQL@ -d $SID -c "CREATE INDEX i_o_custkey ON orders (o_custkey);" || exit 1
@PSQL@ -d $SID -c "CREATE INDEX i_s_nationkey ON supplier (s_nationkey);" || exit 1
@PSQL@ -d $SID -c "CREATE INDEX i_ps_partkey ON partsupp (ps_partkey);" || exit 1
@PSQL@ -d $SID -c "CREATE INDEX i_ps_suppkey ON partsupp (ps_suppkey);" || exit 1
@PSQL@ -d $SID -c "CREATE INDEX i_n_regionkey ON nation (n_regionkey);" || exit 1
@PSQL@ -d $SID -c "CREATE INDEX i_l_commitdate ON lineitem (l_commitdate);" || exit 1

exit 0

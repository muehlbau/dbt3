#!/bin/bash

DIR=`dirname $0`
. ${DIR}/pgsql_profile || exit 1

DB_PARAM=$1

CMD="@PG_CTL@ -D ${PGDATA} start -l @TOPDIR@/run/db_logfile.txt"
if [ $# -eq 1 ]; then
	${CMD} -o "${DB_PARAM}" || exit 1
else
	${CMD} || exit 1
fi


sleep 10

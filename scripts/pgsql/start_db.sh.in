#!/bin/bash

DIR=`dirname $0`
. ${DIR}/pgsql_profile || exit 1

DB_PARAM=$1

CMD="@PG_CTL@ -D ${PGDATA}/${SID} start -l @TOPDIR@/run/db_logfile.txt"
if [ "$db_param" ]; then
	CMD="${CMD} -o ${DB_PARAM}"
fi

${CMD}

sleep 10

db_param=$1
if [ ! -d @TOPDIR@/run ]; then
	mkdir @TOPDIR@/run
	chown -R pgsql:pgsql @TOPDIR@/run
fi
if [ "$db_param" ]; then
	@PG_CTL@ start -l @TOPDIR@/run/db_logfile.txt -o "$db_param"
else
	@PG_CTL@ start -l @TOPDIR@/run/db_logfile.txt
fi
sleep 10
